{% extends "admin/layout.njk" %}
{% block content %}
    <form action="/admin/users?_method=PUT" method="post">
        <div class="admin-title">
            <h1>Editar usuário</h1>
            <div class="buttons">
                <input type="hidden" name="id" value="{{user.id}}">
                <button type="submit" class="button">Salvar</button>
            </div>
        </div>
        {% include "admin/users/form.njk" %}
    </form>
    <form action="/admin/users?_method=DELETE" method="post">
        <input type="hidden" name="id" value="{{user.id}}">
        <button type="submit" class="button delete" onclick="ConfirmDelete.user(event)">Deletar</button>
    </form>
{% endblock content %}